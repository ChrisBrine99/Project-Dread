// 
/*if (painSoundIndex != noone && audio_is_playing(painSounds[painSoundIndex])){
	audio_stop_sound(painSounds[painSoundIndex]);
}

// 
var _numSounds, _prevSoundIndex;
_numSounds = array_length(painSounds) - 1;
_prevSoundIndex = painSoundIndex;
while(_prevSoundIndex == painSoundIndex) {painSoundIndex = irandom(_numSounds);}

// 
var _volume, _pitch;
_volume = random_range(0.75, 1.15);
_pitch = 1 + random_range(-0.075, 0.075);
audio_play_sound_at_ext(x, y - 16, painSounds[painSoundIndex], 0, _volume, _pitch, 75, 225, 1, false);

//
camera_set_shake(8, 15);*/
